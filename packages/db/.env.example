# Copy to .env. The .env file is gitignored.
# Prisma reads this when running migrate/generate commands from this directory.
DATABASE_URL="postgresql://synctip:change-me-locally@localhost:5432/synctip_dev"
