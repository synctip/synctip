# Postgres credentials consumed by docker-compose.yml for local dev.
# Copy to .env and customize. The .env file is gitignored.
POSTGRES_USER="synctip"
POSTGRES_PASSWORD="change-me-locally"
POSTGRES_DB="synctip_dev"
